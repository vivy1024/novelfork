# 系统架构总览

**版本**: v1.0.0
**创建日期**: 2026-05-10
**状态**: ✅ 当前有效
**文档类型**: current

---

## Monorepo 结构

```
novelfork/
├── packages/core/            # 通用基础设施（storage/llm/state/hooks/mcp/runtime）
├── packages/studio/          # Web 工作台（Hono API + React SPA）
├── packages/cli/             # CLI 工具（novelfork 命令）
├── packages/novel-plugin/    # 小说领域插件（engine/routes/handlers/pages）
├── packages/fitness-plugin/  # 健身领域插件骨架（证明可扩展性）
├── docs/                     # 项目文档
├── .kiro/                    # Kiro steering 配置
└── package.json              # 根 workspace 配置
```

五个包通过 pnpm workspace 管理。`core` 是通用基础设施层，`studio` 和 `cli` 依赖 `core`，`novel-plugin` 和 `fitness-plugin` 作为领域插件依赖 `core` 并被 `studio` 加载。

## 技术栈

| 层 | 技术 |
|---|---|
| 运行时 | Bun |
| 语言 | TypeScript（strict） |
| 前端框架 | React 19 + TanStack Router |
| 后端框架 | Hono |
| 数据库 | SQLite（bun:sqlite） |
| 构建 | Vite（前端）+ Bun compile（exe） |
| 包管理 | pnpm workspace |

## 整体数据流

```
┌─────────────────────────────────────────────────────────────────┐
│                         用户                                     │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  React SPA（浏览器）                                             │
│  ShellSidebar + RouteMountPoint + ConversationSurface           │
└──────────────────────────┬──────────────────────────────────────┘
                           │ HTTP / WebSocket
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  backend-contract 层                                             │
│  ContractClient → resourceClient / sessionClient /              │
│  writingActionClient / providerClient                           │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  Hono API 服务器（packages/studio/src/api/）                     │
│  routes/ → lib/ services → core + novel-plugin                  │
└──────────┬───────────────┬──────────────────┬───────────────────┘
           │               │                  │
           ▼               ▼                  ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────────┐
│  LLM 提供商   │  │  SQLite DB   │  │  文件系统          │
│  (OpenAI等)   │  │  (存储层)     │  │  (真相文件/章节)   │
└──────────────┘  └──────────────┘  └──────────────────┘
```

## packages/core 职责

通用基础设施层，不依赖任何 HTTP 框架或 UI 框架，纯 TypeScript 逻辑。提供存储、LLM、状态管理、hooks、MCP、运行时等通用能力。

| 模块 | 职责 |
|---|---|
| `storage/` | 存储层：SQLite schema、15 个 migration、repositories（kv/session/message/request_log） |
| `llm/` | LLM 客户端：createLLMClient、chatCompletion、chatWithTools、Prompt Cache |
| `state/` | 状态管理：StateManager、MemoryDB、projections |
| `hooks/` | 生命周期钩子系统 |
| `mcp/` | MCP 协议客户端 |
| `runtime/` | 进程适配器、沙箱执行（none/basic/strict） |
| `models/` | 数据模型：BookConfig、ChapterMeta、ProjectConfig、RuntimeState 等 |
| `registry/` | 工具注册表 + 命令注册表 + ToolSearch |
| `plugins/` | 插件系统 |
| `notify/` | 通知分发（飞书/企微/Telegram/Webhook） |
| `relay/` | 本地中继 |
| `utils/` | 工具函数集 |

## packages/novel-plugin 职责

小说领域插件，包含所有小说写作相关的引擎、路由、服务和前端页面。

| 模块 | 职责 |
|---|---|
| `engine/pipeline/` | 写作管线主引擎：PipelineRunner（writeNextChapter、writeDraft、reviseDraft 等） |
| `engine/agents/` | 写作 Agent 集合（20+ 个专业 Agent） |
| `engine/jingwei/` | 经纬系统（世界观/角色/事件/设定管理，SQLite story_jingwei_entry/section 表） |
| `engine/filter/` | AI 味过滤器（规则引擎 + 朱雀 API） |
| `engine/compliance/` | 平台合规检查（敏感词/AI 声明/格式） |
| `engine/presets/` | 写作预设库（流派/文风/节拍/逻辑风险） |
| `engine/tools/` | 写作工具集（节奏/对话/进度/弧线/冲突/钩子） |
| `engine/bible/` | 设定百科：角色/事件/设定/世界模型 repositories + 上下文构建 |
| `routes/` | 小说领域 API 路由（ai/jingwei/writing-modes/pipeline/filter/compliance/bible 等） |
| `handlers/` | 业务服务（cockpit/candidate/pgi/guided-generation/novel-init/novel-audit 等） |
| `pages/` | 前端页面（writing-workbench 全部组件） |

## packages/studio 职责

Web 工作台，同时包含 Hono 后端和 React 前端。

| 层 | 路径 | 职责 |
|---|---|---|
| API 服务器 | `src/api/` | Hono 路由 + 服务层，对外暴露 REST API 和 WebSocket |
| API 路由 | `src/api/routes/` | 按领域拆分：session、pipeline、providers、books、writing-modes 等 |
| API 服务 | `src/api/lib/` | 业务逻辑：cockpit-service、session-chat-service、provider-manager 等 |
| 前端入口 | `src/app-next/` | React SPA：shell、backend-contract、writing-workbench、agent-conversation |
| 前端组件 | `src/components/` | UI 组件：ToolCall、Routines、Bible、jingwei 等 |
| 共享类型 | `src/shared/` | 前后端共享的类型定义和契约 |

## packages/cli 职责

命令行工具，提供 `novelfork` 命令。

| 命令 | 功能 |
|---|---|
| `write` | 写下一章 |
| `draft` | 生成草稿 |
| `audit` | 连续性审计 |
| `detect` | AI 味检测 |
| `revise` | 修订章节 |
| `chat` | headless 对话 |
| `studio` | 启动 Web 工作台 |
| `init` | 初始化项目 |
| `import` | 导入章节 |
| `status` | 查看项目状态 |
| `radar` | 市场雷达 |

CLI 直接调用 `core` 的 PipelineRunner，不经过 HTTP 层。

## 部署模型

```
┌────────────────────────────────────────┐
│  novelfork-v1.0.0-windows-x64.exe      │
│                                        │
│  Bun compile 单文件产物                 │
│  内嵌：Hono API + Vite 构建的 SPA      │
│  运行时：本地 SQLite + 本地文件系统      │
│  数据库：~/.novelfork/novelfork.db      │
│  端口：localhost:3000（默认）            │
└────────────────────────────────────────┘
```

- 本地单机运行，无需外部数据库或容器
- exe 启动后同时提供 API 服务和静态前端
- LLM 调用通过配置的 API endpoint 外发（支持 Sub2API 网关）
- 数据存储：SQLite 主数据库（~/.novelfork/novelfork.db，15 个 migration）+ 文件系统（真相文件、章节 markdown）
- Agent 运行时对标 Claude Code：CLAUDE.md 读取、LLM 压缩摘要、Subagent、后台任务、Prompt Cache、ToolSearch、Skills、MCP、沙箱、Browser、Terminal

## 设计决策

1. **core 与 novel-plugin 分离**：core 是通用基础设施（存储/LLM/状态/hooks/MCP/runtime），novel-plugin 包含所有小说领域代码（引擎/路由/服务/页面）。这样 core 可被其他领域插件复用（如 fitness-plugin），而小说功能完全内聚在 novel-plugin 中。

2. **backend-contract 层**：前端不直接拼 URL 或处理 HTTP 细节，而是通过 ContractClient + 领域客户端统一管理。每个请求携带 capability 元数据，前端可据此决定 UI 状态（启用/禁用/只读/预览）。

3. **SQLite 而非远程数据库**：本地单机场景，SQLite 足够且零运维。通过 Drizzle ORM 管理 schema 和 migration。

4. **Bun compile**：将整个应用编译为单个 exe，用户下载即用，无需安装 Node.js 或 Bun 运行时。
