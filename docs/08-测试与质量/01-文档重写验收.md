**版本**: v3.0.0
**创建日期**: 2026-06-26
**更新日期**: 2026-06-26
**状态**: current
**文档类型**: current

# 文档重写验收

## 范围

本次验收对应 `.kiro/specs/docs-rewrite-v3/`。

## 保留规则

- `docs/learning/`：学习中心与 LearningGuide 运行时文档，保留，不纳入普通 docs header 规则。
- `docs/codegraph/`：`bun run codegraph` 生成物，保留，不手改生成内容。

## 已完成项

- `docs/README.md` 已收口为唯一总入口，并增加当前事实入口与特殊目录说明。
- `docs/01-codewiki/` 保持第一代码事实入口。
- 普通 docs 目录均具备 `README.md` 与标准 header。
- 项目入口引用已同步：`README.md`、`CLAUDE.md`、`AGENTS.md`、`CONTRIBUTING.md`、`.kiro/specs/README.md`。
- 历史参考资料保持 `reference` 语义，不作为当前事实入口。

## 验证命令

```bash
bun run docs:verify
bun run docs:drift
```

验收要求：两项均通过。
