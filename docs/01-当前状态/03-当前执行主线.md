# 当前执行主线

**版本**: v1.0.2
**创建日期**: 2026-04-28
**更新日期**: 2026-05-06
**状态**: ✅ 当前有效
**文档类型**: current

---

## 当前 spec 状态

| Spec | 角色 | 当前口径 |
|---|---|---|
| `.kiro/specs/backend-contract-v1/` | 当前 active 主线第一阶段 | 已完成（9/9）；已建立能力矩阵、typed contract client、session/resource/provider/writing action 领域 client、session WebSocket helper、资源树 adapter、写作动作 adapter、fetchJson 桥接与验证 guard。 |
| `.kiro/specs/frontend-refoundation-v1/` | 当前 active 主线第二阶段 | 已完成（12/12）；已正式计入 Backend Contract client、Agent Shell 路由壳、Conversation runtime、单栏 Conversation surface、模型/权限状态栏动作、Tool Result Renderer Registry、Workbench 资源树、canvas/viewer 与写作动作入口；`main.tsx` 与 `StudioNextApp.tsx` 主入口已切断旧三栏默认依赖，失败三栏实验已从 Studio typecheck 构建路径正式退役，并完成 route/retirement guard、app-next Vitest、TypeScript、docs verify 与浏览器冒烟。 |
| `.kiro/specs/frontend-live-wiring-v1/` | 当前 active 主线第三阶段 | 已完成（10/10）；`/next/narrators/:sessionId` 已接 live conversation runtime、模型池/权限/推理配置、工具结果 renderer 与 session tool 确认刷新；`/next/books/:bookId` 已接 resource contract、保存、只读能力、透明 unsupported 状态、dirty canvasContext 与写作动作会话跳转；`/next/search`、`/next/routines`、`/next/settings` 已挂载真实页面或真实错误/透明 unsupported 状态，不再使用“稍后接线”占位。 |
| `.kiro/specs/legacy-source-retirement-v1/` | 当前 active 主线第四阶段 | 已完成（10/10）；旧三栏、旧 ChatWindow、未挂载 route 残留和 `/api/chat` 轻量 route 已删除，windowStore 会话事实源 consumer 已迁到 session runtime；exact `POST /api/agent` 已退役，`/api/pipeline`、`/api/monitor`、旧导出与旧 AI panel 继续按 process-memory/unsupported/deprecated 合同透明保留。 |
| `.kiro/specs/conversation-parity-v1/` | 当前 active 主线第五阶段 | 已完成（13/13）；已完成 Claude Code CLI 2.1.69 parity 对照与范围守护、会话 lifecycle service、ResumePicker/ForkDialog UI 接线、Slash Command Registry、`/compact` 产品化、Memory 写入边界、细粒度工具权限策略、Headless stream-json API 与 CLI 会话命令，支持 scoped continue latest、resume by id、归档 readonly/restore、fork session 标题/继承说明、Composer slash 建议、`/help`/`/status`/`/model`/`/permission`/`/fork`/`/resume`/`/compact` 结构化执行、compact summary/budget 展示、会话 memory 只读状态、偏好/项目事实/临时剧情草稿三分法审计、`sessionConfig.toolPolicy` allow/deny/ask 与 `policy-denied`/`permission-required`/`policy-disabled` 结构化结果，`POST /api/sessions/headless-chat` 的 text/stream-json input、NDJSON output、ephemeral/no-session-persistence、permission_request、max turns/budget stop result 与 duration/stop_reason/usage/cost unknown/permission_denials envelope，`novelfork chat` 的 text/json/stream-json/sessionId/pending confirmation 调用、正式章节/Truth/story/narrative apply 写入前 `.novelfork/checkpoints` 快照，以及按 checkpoint 预览 diff/hash/risk、确认门恢复、拒绝/冲突/资源移动失败与 rewind audit；最终文档、能力矩阵与验收已收口，仍排除完整终端 TUI、tmux、Chrome bridge、remote-control 和插件市场。 |
| `.kiro/specs/backend-core-refactor-v1/` | 后端核心整理阶段 | 已完成（10/10）；已建立 `contract-guard.md`，新增 `contract-regression.test.ts` 覆盖 books、sessions、providers、resources、writing actions 的成功、404/400、能力不可用/gate 与脱敏边界，并完成统一错误与状态 helper、storage.ts 只读/非破坏写入/destructive service 拆分、session runtime transport/recovery 内聚拆分、Provider/runtime store 边界收敛、legacy route 依赖调查/deprecated 标记，以及最终 API/存储/README/CHANGELOG 文档与验证收口。 |
| `.kiro/specs/ui-live-parity-hardening-v1/` | 当前 active 硬化主线 | 执行中（6/14）；已完成 Task 1 RED 回归基线、Task 2 资源详情 hydrate、Task 3 保存控制器/dirty guard、Task 4 浏览器资源编辑读回 E2E、Task 5 Shell session/recovery sync 与 Task 6 SettingsTruthModel/设置分组。当前 `/next/books/:bookId` 打开章节/Story/Truth/草稿 preview 会先读取真实详情，保存后回读详情；`e2e/workbench-resource-edit.spec.ts` 已覆盖真实浏览器打开章节→修改→保存→刷新读回；工作台动作新建 session 会同步左侧叙述者列表，recovery notice 显示归一化状态和 cursor；模型设置页现在按 SettingsFact 展示来源/状态/API，隐藏无 schema 来源的 Codex 推理强度，并在运行控制保存后回读 `/api/settings/user`；provider callable 与对话透明化仍按后续任务执行。 |
| `.kiro/specs/novelfork-ui-v1/` | 被取代的过渡参考 | 保留“sidebar + 全宽对话”方向参考，不再作为实现主线。 |
| `.kiro/specs/archive/agent-native-workspace-v1/` | 已完成历史主线 | 23/23 已完成；确立 session-first 工作台、session tools、工具结果 renderer、PGI / guided / candidate / Narrative Line 链路，并完成测试矩阵、non-goals 回归与最终收尾检查。 |
| `.kiro/specs/archive/novel-creation-workbench-complete-flow/` | 已完成历史主线 | Phase 0-7 已完成，覆盖小说创作工作台完整闭环、透明过渡收敛和最终维护验收。 |
| `.kiro/specs/archive/longform-cockpit-v1/` | 历史规划资产 | 长篇驾驶舱不再作为右侧主入口执行；可复用为 cockpit 工具结果卡片和中间画布组件。 |

其他 spec 已移入 `.kiro/specs/archive/`，只作为历史、参考或复用资产来源，不再作为当前任务入口。当前 active spec 索引以 `.kiro/specs/README.md` 为准。

## 已归档但仍需引用的事实来源

| 归档 spec | 当前用途 |
|---|---|
| `.kiro/specs/archive/docs-system-rearchitecture/` | 文档目录结构、迁移映射和 `docs:verify` 规则来源。 |
| `.kiro/specs/archive/project-wide-real-runtime-cleanup/` | 反 mock、transparent unsupported、mock debt ledger 与真实运行时边界来源。 |
| `.kiro/specs/archive/ui-quality-cleanup/` | app-next UI 清理历史和可复用 UI 约束来源。 |
| `.kiro/specs/archive/novel-bible-v1/`、`.kiro/specs/archive/onboarding-and-story-jingwei/` | 经纬、问卷、生成前追问、核心设定变更等底座参考。 |
| `.kiro/specs/archive/writing-modes-v1/`、`.kiro/specs/archive/writing-tools-v1/` | 写作模式和写作工具底座参考。 |

## 当前优先级

1. `ui-live-parity-hardening-v1` 执行中（6/14）：资源详情 hydrate、保存防覆盖、浏览器保存刷新读回 E2E、Shell session/recovery sync 与 SettingsTruthModel/设置分组已完成，下一步优先推进 provider callable，再推进对话透明化和 Claude/Codex parity 守护；所有 UI current 声明必须绑定真实合同或浏览器证据。
2. `conversation-parity-v1` 已完成（13/13）：覆盖 Claude Code CLI 2.1.69 parity baseline、会话 continue/latest/resume、归档 readonly/restore、fork service、ResumePicker/ForkDialog UI、Slash Command Registry、`/compact`、Memory 写入边界、细粒度工具权限策略、Headless stream-json API、CLI 会话命令、资源 checkpoint、Rewind preview/apply、会话执行结果与 usage envelope，并完成最终文档、能力矩阵与验收收口；范围仍只补齐对 NovelFork 有价值的 resume/fork、slash、compact、memory、tool policy、headless、checkpoint/rewind 能力，不复制终端 TUI。
3. 维护 `backend-contract-v1` 合同边界：新增后端能力前先补矩阵、共享类型、contract client / adapter 与验证 guard，不在 app-next 组件内散写未登记 API。
4. 保持 `legacy-source-retirement-v1` 已完成口径：旧三栏、旧 ChatWindow、未挂载 route 残留、轻量 `/api/chat` 与 exact `POST /api/agent` 已退役；`/api/pipeline`、`/api/monitor`、旧导出入口、旧 AI 面板直连接口只允许标注 transparent/deprecated/unsupported 等非 current 状态，不得宣传为真实可用能力，恢复前必须重新创建 current contract、tests 与 consumer。
5. 保持 `agent-native-workspace-v1` 与 `web-agent-runtime-v1` 已完成口径：session tools、工具循环、确认门、Guided/PGI/candidate/Narrative Line 和 Web Agent Runtime 均作为后续重建资产复用。
6. `backend-core-refactor-v1` 已完成 10/10：合同守护、核心回归测试、错误/状态 helper 收敛、storage 只读/非破坏写入/destructive service 拆分、session runtime transport/recovery 内聚拆分、Provider/runtime store 边界收敛、legacy route 依赖调查和最终文档/验收；后续新增后端能力仍必须先调查、再分阶段最小改动，不大规模破坏后端 route。
7. 保持 `novel-creation-workbench-complete-flow` Phase 0-7 的完成口径，不恢复 mock/fake/noop 假成功。
8. 历史长篇驾驶舱能力只作为 cockpit tools / 工具结果卡片 / 中间画布组件复用，不再恢复为右侧主入口。

## 执行约束

- 文档体系以 `docs/` 当前目录结构和 `.kiro/specs/README.md` 为准。
- 业务事实以当前主线 spec、Backend Contract、代码、测试和运行状态为准。
- current 文档只能描述当前真实状态；规划能力必须链接到对应主线任务。
- 未接入能力必须 transparent unsupported，不能假成功。

## 相关入口

- [文档体系规范](../00-文档治理/01-文档体系规范.md)
- [Studio能力矩阵](./02-Studio能力矩阵.md)
- [项目当前状态](./01-项目当前状态.md)
- [小说创作流程](../03-产品与流程/01-小说创作流程.md)
/01-小说创作流程.md)
/01-小说创作流程.md)
��流程.md)
/01-小说创作流程.md)
