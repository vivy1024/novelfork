---
title: 运行时能力
summary: CLAUDE.md 规则读取、LLM 压缩摘要、文件安全检查等运行时机制
tags: [运行时, CLAUDE.md, 压缩, 安全, token]
---

# 运行时能力

> NovelFork 在运行过程中自动执行的智能机制，无需手动干预。

## CLAUDE.md 规则读取

系统启动时自动加载三层规则文件，按优先级合并：

1. **全局规则** `~/.novelfork/CLAUDE.md` — 适用于所有项目的通用偏好（写作风格、禁忌词等）
2. **项目规则** 项目根目录 `CLAUDE.md` — 当前书籍的专属设定（世界观、角色约束）
3. **目录规则** `.claude/rules/*.md` — 细粒度规则，按目录生效

规则会注入到 Agent 上下文中，指导 AI 的行为边界。你只需编辑对应文件，下次对话自动生效。

## LLM 智能压缩摘要

当对话历史接近上下文窗口上限时，系统自动调用摘要模型将早期对话压缩为精炼摘要。这不是简单截断，而是保留关键决策和上下文的智能压缩，确保长对话不丢失重要信息。

## Staleness Check（防覆盖）

文件写入前，系统检查该文件自上次读取后是否被外部程序修改过。如果检测到外部变更，会提示你确认而非直接覆盖，避免丢失手动编辑的内容。

## 文件 Dedup（省 token）

同一文件在对话中多次被引用时，系统只在首次发送完整内容，后续引用复用已有内容。这显著减少发送给模型的 token 数量，降低 API 费用并加快响应速度。
