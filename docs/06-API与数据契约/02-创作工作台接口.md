# 创作工作台接口

**版本**: v1.0.1
**创建日期**: 2026-04-28
**更新日期**: 2026-04-28
**状态**: ✅ 当前有效
**文档类型**: current

---

## 本文定位

本文整理 NovelFork Studio 创作工作台当前已经存在的真实接口、透明过渡语义和仍未补齐的边界。它比 [Studio API总览](./01-Studio%20API总览.md) 更聚焦“作者在工作台里会直接碰到什么接口”，但仍然不是逐字段穷举 schema 文档。

接口口径必须反查 `packages/studio/src/api/routes/` 的真实实现；本文只记录当前事实，不把未来 spec 目标提前写成已完成。

## 状态术语

| 术语 | 含义 |
|---|---|
| `current` | route 已存在，且返回真实数据、真实持久化结果或真实错误 |
| `process-memory` | 只保存在当前进程内存，重启后不保证存在 |
| `prompt-preview` | 只返回 prompt / promptPreview，不代表已经生成或写入正文 |
| `chunked-buffer` | 已拿到完整 AI 结果后再切块发送，不是上游原生流式 |
| `unsupported` | 当前未接入，必须返回明确错误或透明占位，不得假成功 |

## 创作工作台接口分组

| 分组 | 主要路径 | 当前口径 |
|---|---|---|
| 作品与章节 | `/api/books*`、`/api/project*` | current |
| 候选稿与草稿边界 | `/api/books/:id/candidates*`、`/api/books/:id/drafts*`、`/api/books/:id/draft` | candidates/drafts 资源 CRUD current；`/draft` 仍是 AI 异步动作 |
| 故事经纬与引导式生成 | `/api/books/:bookId/jingwei/*`、`/api/questionnaires*`、`/api/books/:bookId/core-shifts*` | current |
| AI 动作 | `/api/books/:id/write-next`、`/api/books/:id/audit/:chapter` 等 | current / transparent 混合 |
| 写作模式 | `/api/books/:bookId/inline-write` 等 | transparent：当前主要是 `prompt-preview` |
| 写作工具 | `/api/books/:bookId/hooks*`、`/api/books/:bookId/health` 等 | current / transparent 混合 |
| Provider 与模型池 | `/api/providers*` | current |
| 导出与运行事件 | `/api/books/:id/export`、`/api/books/:id/export-save`、`/api/events` | current / 部分 legacy |

## 1. 作品、章节与项目配置

### 作品读取与创建

| 方法 | 路径 | 作用 | 当前响应/边界 | 持久化 |
|---|---|---|---|---|
| `GET` | `/api/books` | 列出作品 | 返回 `{ books }`，聚合 `book.json` 与章节索引统计 | 真实读取 |
| `GET` | `/api/books/:id` | 读取作品详情 | 返回 `{ book, chapters, nextChapter }`；不存在时 404 | 真实读取 |
| `POST` | `/api/books/create` | 创建作品 | 返回 `{ status: "creating", bookId, defaultSession, defaultSessionSnapshot }`；模型缺失时会走本地 scaffold fallback，而不是假正文 | 真实写入 |
| `GET` | `/api/books/:id/create-status` | 轮询建书状态 | 缺失返回 `{ status: "missing" }` + 404 | 当前进程状态 + 建书产物 |
| `PUT` | `/api/books/:id` | 更新作品配置 | 支持 `chapterWordCount`、`targetChapters`、`status`、`language` | 真实写入 |
| `DELETE` | `/api/books/:id` | 删除作品 | 删除本地目录与 SQLite `book` 记录 | 真实删除 |

### 章节读取、保存与状态

| 方法 | 路径 | 作用 | 当前响应/边界 | 持久化 |
|---|---|---|---|---|
| `GET` | `/api/books/:id/chapters/:num` | 读取单章正文 | 返回 `{ chapterNumber, filename, content }`；找不到返回 404 | 真实读取 |
| `POST` | `/api/books/:id/chapters` | 新建章节 | 请求体 `{ title?: string, afterChapterNumber?: number }`；返回 `{ chapter: ChapterSummary }`；创建章节 Markdown 与章节索引 | 真实写入 |
| `PUT` | `/api/books/:id/chapters/:num` | 保存单章正文 | 请求体 `{ content }`；成功返回 `{ ok: true, chapterNumber }` | 真实写入 |
| `POST` | `/api/books/:id/chapters/:num/approve` | 标记章节通过 | 返回 `{ ok: true, status: "approved" }` | 真实写入章节索引 |
| `POST` | `/api/books/:id/chapters/:num/reject` | 回滚/拒绝章节 | 返回 `{ ok, status: "rejected", rolledBackTo, discarded }` | 真实写入 |
| `POST` | `/api/books/:id/import/chapters` | 导入章节文本 | 当前已存在真实 route，导入结果应进入章节资源树；失败返回真实错误 | 真实写入 |

### Story / Truth 文件

| 方法 | 路径 | 作用 | 当前响应/边界 | 持久化 |
|---|---|---|---|---|
| `GET` | `/api/books/:id/truth` | 列出 story 目录中的 truth 文件 | 返回 `{ files }`，包含 `name`、`size`、`preview` | 真实读取 |
| `GET` | `/api/books/:id/truth/:file` | 读取指定 truth 文件 | 非法文件名返回 400；不存在时返回 `{ file, content: null }` | 真实读取 |
| `PUT` | `/api/books/:id/truth/:file` | 保存指定 truth 文件 | 非法文件名返回 400 | 真实写入 |
| `GET` | `/api/project` | 读取项目配置 | 读取 `novelfork.json` | 真实读取 |
| `PUT` | `/api/project` | 更新项目配置 | 写回 `novelfork.json` | 真实写入 |

## 2. 候选稿、草稿与 AI Draft 边界

### 候选稿 CRUD 与非破坏性处理

| 方法 | 路径 | 作用 | 当前响应/边界 | 持久化 |
|---|---|---|---|---|
| `GET` | `/api/books/:id/candidates` | 列出候选稿 | 返回 `{ candidates }`，正文来自 `generated-candidates/*.md`；缺正文时该条返回 `content: null` 与 `contentError` | 真实读取 |
| `POST` | `/api/books/:id/candidates` | 新建候选稿 | 请求体至少需要 `title`、`content`、`source`；缺失返回 400 | 真实写入 |
| `POST` | `/api/books/:id/candidates/:candidateId/accept` | 接受候选稿 | `action` 必须是 `merge` / `replace` / `draft`；返回 `{ candidate }` 或 `{ candidate, draft }` | 真实写入 |
| `POST` | `/api/books/:id/candidates/:candidateId/reject` | 拒绝候选稿 | 返回状态更新结果 | 真实写入 |
| `POST` | `/api/books/:id/candidates/:candidateId/archive` | 归档候选稿 | 返回状态更新结果 | 真实写入 |

`accept` 的关键边界：

- `merge` / `replace` 会把候选正文写入正式章节。
- `draft` 会把候选正文写入 `books/<bookId>/drafts/<draftId>.md`，并返回新草稿对象。
- 任何非法 `action` 都返回 400，而不是静默吞掉。

### 当前草稿接口边界

草稿资源 CRUD 已补齐，当前可用接口：

| 方法 | 路径 | 作用 | 当前响应/边界 | 持久化 |
|---|---|---|---|---|
| `GET` | `/api/books/:bookId/drafts` | 列出草稿资源 | 返回 `{ drafts }`，每条包含 `id`、`bookId`、`title`、`content`、`updatedAt`、`wordCount` | 真实读取 |
| `GET` | `/api/books/:bookId/drafts/:draftId` | 读取单个草稿 | 返回 `{ draft }`；不存在返回 404 | 真实读取 |
| `POST` | `/api/books/:bookId/drafts` | 新建草稿 | 请求体支持 `title`、`content`；返回 `{ draft }` | 真实写入 |
| `PUT` | `/api/books/:bookId/drafts/:draftId` | 更新草稿 | 请求体支持 `title`、`content`；返回 `{ draft }` | 真实写入 |

候选稿接受为 `draft` 时会落盘到 `drafts/` 目录，并返回可由上述接口读取的新草稿。

`POST /api/books/:id/draft` 仍是 AI draft 异步动作启动接口，返回 `{ status: "drafting", bookId }`，不等同于草稿资源 CRUD。

## 3. 故事经纬与引导式生成

### 故事经纬结构化接口

| 方法 | 路径 | 作用 | 当前响应/边界 | 持久化 |
|---|---|---|---|---|
| `GET` | `/api/books/:bookId/jingwei/sections` | 列出经纬栏目 | 返回 `{ sections }` | SQLite |
| `POST` | `/api/books/:bookId/jingwei/sections` | 新建栏目 | 缺 `key`/`name` 返回结构化错误 | SQLite |
| `PUT` | `/api/books/:bookId/jingwei/sections/:sectionId` | 更新栏目 | 找不到返回 404 | SQLite |
| `DELETE` | `/api/books/:bookId/jingwei/sections/:sectionId` | 删除栏目 | 软删除，返回 `{ ok: true, id }` | SQLite |
| `GET` | `/api/books/:bookId/jingwei/entries` | 列出条目 | 支持 `sectionId` 过滤 | SQLite |
| `POST` | `/api/books/:bookId/jingwei/entries` | 新建条目 | 缺 `sectionId`/`title` 返回错误 | SQLite |
| `PUT` | `/api/books/:bookId/jingwei/entries/:entryId` | 更新条目 | 返回 `{ entry }` | SQLite |
| `DELETE` | `/api/books/:bookId/jingwei/entries/:entryId` | 删除条目 | 软删除 | SQLite |
| `POST` | `/api/books/:bookId/jingwei/templates/apply` | 应用经纬模板 | 返回 `{ templateId, sections }`；已有栏目会跳过 | SQLite |
| `POST` | `/api/books/:bookId/jingwei/preview-context` | 预览经纬上下文 | 按章节与 tokenBudget 裁剪 | 即时装配 |

### 引导式生成接口

| 方法 | 路径 | 作用 | 当前响应/边界 | 持久化 |
|---|---|---|---|---|
| `GET` | `/api/questionnaires` | 列出问卷模板 | 会先 seed 内置模板 | SQLite |
| `POST` | `/api/books/:bookId/questionnaires/:templateId/responses` | 创建问卷回答 | 返回 `{ response, targetObjectId }`；支持 `draft` / `skipped` / `submitted` | SQLite |
| `PUT` | `/api/books/:bookId/questionnaires/:templateId/responses/:id` | 更新问卷回答 | `status=submitted` 会重新走提交逻辑；其余状态走 update | SQLite |
| `POST` | `/api/books/:bookId/questionnaires/:templateId/ai-suggest` | 生成单题 AI 建议 | 模板/问题不存在返回 404 | 即时计算 |
| `POST` | `/api/books/:bookId/chapters/:chapter/pre-generation-questions` | 生成本章追问 | 返回问题和启发式命中，不写正文 | SQLite + 即时计算 |
| `GET` | `/api/books/:bookId/core-shifts` | 查询核心设定变更 | 可按状态过滤 | SQLite |
| `POST` | `/api/books/:bookId/core-shifts` | 提出核心设定变更 | 记录影响对象和章节 | SQLite |
| `POST` | `/api/books/:bookId/core-shifts/:id/accept` | 接受变更 | 返回 `{ coreShift }` | SQLite |
| `POST` | `/api/books/:bookId/core-shifts/:id/reject` | 拒绝变更 | 返回 `{ coreShift }` | SQLite |

## 4. AI 动作接口

### 当前已接入或已有真实 route 的动作

| 方法 | 路径 | 作用 | 当前边界 |
|---|---|---|---|
| `POST` | `/api/books/:id/write-next` | 生成下一章 | 当前返回 `{ status: "writing", bookId }` 启动异步流程，并通过 `/api/events` 广播 `write:*` 事件；不是同步返回候选稿正文 |
| `POST` | `/api/books/:id/draft` | 触发 AI draft | 返回 `{ status: "drafting", bookId }`；是异步动作启动，不等于草稿资源 CRUD |
| `POST` | `/api/books/:id/audit/:chapter` | 连续性/审校 | 成功直接返回审校结果；找不到章节 404 |
| `POST` | `/api/books/:id/revise/:chapter` | 修订草稿 | 成功直接返回 pipeline revise 结果 |
| `POST` | `/api/books/:id/rewrite/:chapter` | 重写章节 | 当前会先 rollback，再启动异步重写流程 |
| `POST` | `/api/books/:id/resync/:chapter` | 重建章节产物 | 返回 resync 结果 |
| `POST` | `/api/books/:id/detect/:chapter` | 单章 AI 痕迹检测 | 返回分析结果 |
| `POST` | `/api/books/:id/detect-all` | 全书 AI 痕迹检测 | 返回 `{ bookId, results }` |
| `POST` | `/api/ai/complete` | 行内补全 SSE | 返回 `chunked-buffer` 语义，不是上游原生流式 |
| `GET` | `/api/events` | Studio 全局 SSE | 广播 `write:*`、`draft:*`、`audit:*` 等事件 |

### 当前透明边界

- `write-next` 与 `draft` 是**异步动作入口**，当前文档不能把它们写成“同步返回正文”。
- `/api/ai/complete` 会在拿到完整结果后再分块发送，SSE payload 含 `streamSource: "chunked-buffer"`。
- AI 失败时返回真实错误或 SSE `error` event，不能伪造成功结果。

## 5. 写作模式接口

当前写作模式接口**主要是 prompt-preview 路径**：

| 方法 | 路径 | 结果 | 当前口径 |
|---|---|---|---|
| `POST` | `/api/books/:bookId/inline-write` | `mode: "prompt-preview"` + `promptPreview` | continuation / expansion / bridge 只生成 prompt |
| `POST` | `/api/books/:bookId/dialogue/generate` | `mode: "prompt-preview"` + `promptPreview` | 对话生成是 prompt 预览，不写正文 |
| `POST` | `/api/books/:bookId/variants/generate` | `mode: "prompt-preview"` + `promptPreviews[]` | 多版本是 prompt 预览 |
| `POST` | `/api/books/:bookId/outline/branch` | `mode: "prompt-preview"` + `promptPreview` | 大纲分支是 prompt 预览 |
| `POST` | `/api/books/:bookId/outline/branch/:branchId/expand` | `mode: "prompt-preview"` + `promptPreview` | 扩展分支也是 prompt 预览 |
| `POST` | `/api/works/import` | 解析导入文本 | 只返回解析结果，不直接落库 |
| `GET` | `/api/style/personal-profile` | 合并个人文风 profile | `profiles` 缺失或 JSON 非法返回 400 |
| `POST` | `/api/books/:bookId/style/drift-check` | 返回 `drift` | 真实分析结果 |

**当前没有** `POST /api/books/:bookId/writing-modes/apply` 这样的安全应用 route，因此任何“应用到正文/候选/草稿”的 UI 都只能写成 prompt-preview 或 disabled 透明态。

## 6. 写作工具接口

| 方法 | 路径 | 作用 | 当前响应/边界 |
|---|---|---|---|
| `POST` | `/api/books/:bookId/hooks/generate` | 生成章节 hooks | 模型不可用时返回 `{ gate }` + 409 |
| `POST` | `/api/books/:bookId/hooks/apply` | 持久化 hook | 成功返回 `{ persisted: true, file: "pending_hooks.md", hookId }` |
| `GET` | `/api/books/:bookId/pov` | POV 仪表盘 | 基于 story 文件构建 dashboard |
| `GET` | `/api/progress` | 写作进度 | 返回 `{ config, progress, trend }` |
| `PUT` | `/api/progress/config` | 更新进度配置 | 返回 `{ config }` |
| `POST` | `/api/books/:bookId/chapters/:ch/rhythm` | 节奏分析 | 缺正文返回 404 |
| `POST` | `/api/books/:bookId/chapters/:ch/dialogue` | 对话分析 | 缺正文返回 404 |
| `GET` | `/api/books/:bookId/health` | 书籍健康面板 | 真实字段返回 measured metric；未知字段返回 `unknown` |
| `GET` | `/api/books/:bookId/conflicts/map` | 冲突图 | 返回 `{ conflicts }` |
| `GET` | `/api/books/:bookId/arcs` | 人物弧 | 返回 `{ arcs }` |
| `POST` | `/api/books/:bookId/chapters/:ch/tone-check` | 语气漂移检查 | 返回 `{ result }` |

`/api/books/:bookId/health` 是当前“真实计算 + unknown 混合”的典型接口：

- `totalChapters`、`totalWords`、`dailyWords`、`dailyTarget`、`sensitiveWordCount`、`knownConflictCount` 来自真实数据。
- `consistencyScore`、`hookRecoveryRate`、`aiTasteMean`、`rhythmDiversity` 当前明确返回 `unknown`，不能写成满分。

## 7. Provider、模型池与 AI gate

Provider routes 由 `createProvidersRouter()` 挂载到 `/api/providers`：

| 方法 | 路径 | 作用 | 当前响应/边界 |
|---|---|---|---|
| `GET` | `/api/providers` | 列出 provider | 返回脱敏后的 `{ providers }` |
| `GET` | `/api/providers/status` | 返回运行时 provider 状态 | 基于 runtime store |
| `GET` | `/api/providers/models` | 统一模型池 | 只返回 enabled provider/model/可用账号组合 |
| `POST` | `/api/providers` | 新建 provider | 缺 `id/name/type` 返回 400 |
| `PUT` | `/api/providers/:id` | 更新 provider | 不存在返回 404 |
| `POST` | `/api/providers/:id/toggle` | 启停 provider | 返回脱敏 provider |
| `POST` | `/api/providers/reorder` | 调整顺序 | 非法 id 返回 400 |
| `POST` | `/api/providers/:id/models/refresh` | 刷新模型列表 | adapter unsupported / auth 缺失返回失败 envelope |
| `PATCH` | `/api/providers/:id/models/:modelId` | patch 单模型 | 不存在返回 404 |
| `POST` | `/api/providers/:id/models/:modelId/test` | 测试单模型 | 失败时返回 envelope + model；成功返回 `{ success: true, latency, model }` |
| `POST` | `/api/providers/:id/test` | 测试 provider 当前启用模型 | 无启用模型返回 400 |
| `DELETE` | `/api/providers/:id` | 删除 provider | 成功返回 `{ success: true }` |

关键边界：

- 当前 runtime model pool 已是唯一运行时模型来源。
- API 不回传明文 `apiKey` 或 `credentialJson`。
- adapter 未实现时必须返回真实失败 envelope，而不是伪造模型可用。

## 8. 导出与 legacy 边界

| 方法 | 路径 | 作用 | 当前口径 |
|---|---|---|---|
| `GET` | `/api/books/:id/export` | 读取导出内容 | 当前已存在真实 route，但属于 legacy 导出路径，首版并非 spec 里的统一 `POST export` 设计 |
| `POST` | `/api/books/:id/export-save` | 保存导出结果 | 当前已存在真实 route |

当前 spec 里的 `POST /books/:bookId/export` 仍是后续目标；现阶段文档必须写清楚：**工作台已有导出相关 route，但统一导出入口和范围选择还未按 spec 收敛。**

## 9. 当前缺口与禁止写法

### 当前缺口

1. writing modes 安全 apply route 还未落地。
2. `write-next` / `draft` 仍是异步启动接口，不是同步正文接口。
4. 导出仍是当前 route 与后续 spec 设计并存阶段。

### 禁止写法

- 不得把 `prompt-preview` 写成“已生成正文”。
- 不得把 `process-memory` 描述成“重启后仍保留”的长期状态。
- 不得把 `unsupported` 写成“当前可用”。
- 不得把异步 `POST /api/books/:id/draft` 写成草稿资源 CRUD；草稿资源 CRUD 以 `/api/books/:id/drafts*` 为准。

## 10. 验证事实源

- `packages/studio/src/api/routes/storage.ts`
- `packages/studio/src/api/routes/chapter-candidates.ts`
- `packages/studio/src/api/routes/ai.ts`
- `packages/studio/src/api/routes/bible.ts`
- `packages/studio/src/api/routes/jingwei.ts`
- `packages/studio/src/api/routes/writing-modes.ts`
- `packages/studio/src/api/routes/writing-tools.ts`
- `packages/studio/src/api/routes/providers.ts`
- [Studio API总览](./01-Studio%20API总览.md)
资源系统”。

## 10. 验证事实源

- `packages/studio/src/api/routes/storage.ts`
- `packages/studio/src/api/routes/chapter-candidates.ts`
- `packages/studio/src/api/routes/ai.ts`
- `packages/studio/src/api/routes/bible.ts`
- `packages/studio/src/api/routes/jingwei.ts`
- `packages/studio/src/api/routes/writing-modes.ts`
- `packages/studio/src/api/routes/writing-tools.ts`
- `packages/studio/src/api/routes/providers.ts`
- [Studio API总览](./01-Studio%20API总览.md)
