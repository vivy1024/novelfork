# NovelFork 系统架构总览

**更新日期**: 2026-04-20  
**状态**: 当前有效口径

---

## 先看结论

NovelFork 当前不是一个“坏项目”，而是一个：

- **核心写作能力已经很多**
- **平台外壳方向走错了**

因此架构判断必须分成两层：

1. **当前可复用资产**
2. **当前需要回正的平台残留**

---

## 一、当前可复用资产

### 1. 核心写作引擎：`packages/core`
这是当前仓库最值得保留的部分。

已实现的主要能力包括：
- 多 Agent 写作管线
- 章节规划 / 编排 / 写作 / 审计 / 修订
- 运行时状态管理
- Lorebook / memory / hook / summary 等上下文机制
- AI 痕迹检测、敏感词检测、文风分析
- 章节导入、重写、恢复、长度治理

这部分代码代表的是 **NovelFork 的产品能力**，不是偏航点。

### 2. Web 工作台中的可复用 UI 与 API：`packages/studio`
虽然 studio 的平台定位被旧文档写歪了，但其中很多能力仍可复用：
- React 前端组件
- 多面板工作区
- 配置页与管理页的部分实现
- Hono API 组织方式
- 局部的工具执行与可视化能力

可复用不等于当前结构正确。可复用的意思是：
- 这些能力可以迁移到新的单入口应用中
- 不需要全盘重写

### 3. 题材与规则资产
`packages/core/genres/` 以及相关规则读取、书籍约束、审计逻辑，属于长期可复用资产。

### 4. 文档中保留的历史证据
`docs/archive/` 里虽然很多内容已经过时，但它们仍然有一个价值：
- 说明项目曾经怎么判断平台问题
- 说明为什么后来会偏航

---

## 二、当前平台残留

真正需要回正的，不是写作能力，而是下面这些外壳层。

### 1. 多入口结构
当前入口分散在：
- `packages/cli`
- `packages/studio`
- `packages/desktop`

这更像一个开发中的 monorepo，而不像一个完整产品。

### 2. `cli -> studio` 启动链路
当前存在明显的“CLI 去拉起 Web 工作台”的结构。

这会导致：
- 启动路径绕
- 用户体验更像开发工具
- 最终分发困难

### 3. `tsc -> dist` 驱动的运行模式
当前 build 成功，但这套运行方式更适合源码项目，不适合最终产品形态。

### 4. 旧的 PWA/发布叙事
旧文档把平台目标叙述成：
- PWA
- 可安装 Web 工作台
- v0.0.1 发布准备

这套叙事已经失效。

---

## 三、当前事实状态

基于当前代码与验证结果，可做如下判断。

### 构建状态
- `packages/core` 可编译
- `packages/cli` 可编译
- `packages/studio` client/server 可编译

### 测试状态
- core 大部分测试通过，但仍有失败
- cli/studio 有明显测试与基础设施问题
- 失败更多集中在：
  - 测试环境配置
  - mock 不完整
  - studio runtime / 路径解析

### 平台状态
当前仓库更像：
- 一套有较强写作能力的 monorepo 工程
- 而不是一个交付完成的本地单体应用

所以“当前状态”应该被描述为：

> **核心能力可用，平台外壳未回正。**

而不是“生产就绪”。

---

## 四、目标架构

### 目标结构

```text
novelfork (single app)
  ├─ app entry (Bun)
  ├─ local server (Hono)
  ├─ embedded frontend assets
  ├─ writing engine (migrated/reused from packages/core)
  ├─ local data (SQLite)
  └─ browser UI
```

### 目标启动模型

```text
用户启动 novelfork
  -> 本地服务启动
  -> 打开浏览器
  -> React UI 接管交互
  -> Hono API 调写作引擎
  -> SQLite 与本地文件保存状态
```

### 目标分发模型

```text
源码开发 -> Bun 运行
产品分发 -> bun compile -> 单可执行文件
```

---

## 五、从当前到目标的差距

### 差距 1：入口仍然分裂
需要把当前多入口逐步收敛为一个主入口。

### 差距 2：运行模式仍围绕源码与构建产物
需要从“先 build，再由不同包各自运行”转向“单入口应用驱动”。

### 差距 3：分发方案没有定型
需要把 `bun compile` 确立为主分发路线。

### 差距 4：文档与实现长期不一致
这次 docs 回正就是先解决这个问题，让后续改造有统一依据。

---

## 六、当前阶段的架构原则

在代码真正迁移前，所有架构判断遵循：

1. **保留核心写作引擎**
2. **允许重写错误的产品外壳**
3. **先统一单体应用目标，再做具体代码迁移**
4. **不再把旧的 PWA/发布流程当成目标态**

---

## 七、目录角色的重新理解

### 当前仍保留
- `packages/core`：产品核心资产
- `packages/studio`：可迁移 UI/API 资产
- `packages/cli`：过渡期命令能力
- `packages/desktop`：历史残留，暂不作为主路线

### 未来倾向
- `core` 能力下沉为主应用内部模块
- `studio` 成为前端界面层
- `cli` 可能并入主入口或只保留调试用途
- `desktop` 是否保留，取决于单 exe 路线是否完全覆盖需求

---

## 八、窗口 / 会话 / 快照三者关系（Package 4 / 6 收口口径）

NovelFork 的前端把"用户看到的内容"拆成三层，彼此解耦：

- **窗口 (`chatWindow` in `useWindowStore`)**：只是"打开/最小化/位置"的视图壳。`persist` 仅保留布局与 `sessionId` 关联，不承载任何消息内容。
- **会话 (`NarratorSessionRecord`)**：后端是消息链、模式、参数的唯一事实源。关闭窗口不销毁会话，关掉最后一个窗口后会话仍在 `SessionCenter` 卡片里可重新打开。
- **快照 (`NarratorSessionChatSnapshot` + `windowRuntimeStore`)**：后端广播的增量/回放快照驱动前端渲染，`recoveryState` 走 `idle / recovering / reconnecting / replaying / resetting` 五态，由 `windowRecoveryPresentation.ts` 提供统一的徽标/文案展示。

推论：**窗口是视图、会话是对象、快照是协议**。任何 UI 层新的"打开/关闭/恢复"逻辑都应该基于这三层心智，不要在窗口持久化里偷藏消息，也不要绕开快照直写会话记录。

---

## 相关文档

- [平台纠偏说明.md](./平台纠偏说明.md)
- [技术栈选型.md](./技术栈选型.md)
- [../../04-开发指南/05-调研规划/02-平台回正规划.md](../../04-开发指南/05-调研规划/02-平台回正规划.md)
- [../../04-开发指南/05-调研规划/03-平台迁移待办清单.md](../../04-开发指南/05-调研规划/03-平台迁移待办清单.md)
