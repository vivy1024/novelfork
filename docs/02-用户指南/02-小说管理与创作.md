# 小说管理与创作

**版本**: v1.0.0
**创建日期**: 2026-05-10
**更新日期**: 2026-05-18
**状态**: ✅ 当前有效
**文档类型**: current

---

## 创建作品

### 新建书籍流程

1. 点击侧边栏"叙事线（书籍）"分组的新建按钮
2. 选择或创建仓库目录（必填）
3. 输入书名（可选，留空则使用目录名）
4. 选择题材（可选）
5. 确认创建

支持的题材：玄幻、仙侠、都市、科幻、其他。

### 新书引导向导（NewBookGuide）

新书进入工作台后，自动显示引导式创作向导：

- **11 道问题**：覆盖题材、文风、目标读者、核心卖点、世界观、主角设定等
- **每题三种模式**：
  - 预设选择：从推荐选项中快速选择
  - 自定义：手动输入个性化内容
  - 跳过随机：跳过该题，系统随机填充合理默认值
- 引导完成后自动生成初始真相文件，进入正常驾驶舱视图

---

## 工作台布局

Studio 采用三栏布局：

```
┌──────────────┬────────────────────────────┬──────────────────┐
│  左：资源树   │     中：画布（Canvas）      │  右：叙述者会话   │
│              │                            │                  │
│  章节列表     │  章节编辑器 / 驾驶舱       │  消息流           │
│  经纬资料     │  候选稿查看器              │  Composer         │
│  大纲与设定   │  经纬条目编辑器            │  NarratorStatusBar│
│  真相文件     │  叙事线快照                │                  │
└──────────────┴────────────────────────────┴──────────────────┘
```

- **左栏**（WorkbenchResourceTree）：资源树导航，按类型分组展示所有写作资源
- **中栏**（WorkbenchCanvas）：根据选中资源渲染对应的查看器/编辑器
- **右栏**（ConversationSurface）：固定的叙述者 AI 对话面板

---

## 资源树结构

资源树按以下类型组织：

| 节点类型 | 说明 |
|---------|------|
| `book` | 作品根节点，含驾驶舱总览入口 |
| `chapter` | 章节，按章节号排序 |
| `candidate` | 候选稿，AI 生成的待审内容 |
| `draft` | 草稿，从候选稿接受或手动创建 |
| `outline` | 大纲文件 |
| `setting` | 设定文件（世界观、规则等） |
| `truth` | 真相文件（ground truth） |
| `jingwei-section` | 经纬分区（人物、地点、物品等） |
| `jingwei-entry` | 经纬条目（具体的人物/地点/物品） |
| `narrative-snapshot` | 叙事线快照 |

---

## 章节管理

### 创建章节

通过工作台或 API 创建新章节，章节文件存储在 `books/{bookId}/chapters/` 目录下。

### 编辑与保存

中栏画布提供基于 TipTap 的富文本编辑器：

- **Markdown 双向转换**：内部以 Markdown 存储，编辑时渲染为富文本，保存时自动转回 Markdown
- **自动保存**：编辑内容自动保存，无需手动操作
- **字数统计**：编辑器底部实时显示当前章节字数

> 所有 AI 写作操作（续写/扩写/改写/补写）通过右侧 Agent 对话面板的快捷按钮触发。

### 批准/拒绝

章节经过连续性审校后，可标记为：
- **批准**（approved）：审校通过，计入已完成章节
- **拒绝**（failed）：审校发现问题，需要修订

---

## 经纬系统

经纬系统是 NovelFork 的核心设定管理机制，用于维护人物、地点、物品、势力等结构化设定数据。数据存储在 SQLite 中。

### 数据写入

经纬条目通过 `jingwei.upsert_entry` 工具写入，支持创建和更新。AI Agent 在写作过程中会自动维护经纬数据。

### 前端查看

经纬数据在前端经纬页面查看和管理，支持：

- 分区管理（人物、地点、物品、势力等）
- 条目 CRUD
- 可见性规则配置
- 上下文预览

### 分区属性

| 字段 | 说明 |
|------|------|
| `key` | 唯一标识键（如 `characters`、`locations`） |
| `name` | 显示名称 |
| `description` | 分区描述 |
| `enabled` | 是否启用 |
| `showInSidebar` | 是否在侧栏显示 |
| `participatesInAi` | 是否参与 AI 上下文组装 |
| `defaultVisibility` | 默认可见性规则：`global`/`nested`/`tracked` |

### 条目属性

| 字段 | 说明 |
|------|------|
| `title` | 条目标题（如角色名） |
| `contentMd` | Markdown 格式的详细描述 |
| `tags` | 标签数组 |
| `aliases` | 别名数组（用于 AI 识别） |
| `relatedChapterNumbers` | 关联章节号 |
| `visibilityRule` | 可见性规则（控制 AI 何时能看到此条目） |
| `participatesInAi` | 是否参与 AI 上下文 |

---

## 真相文件与大纲设定

- **真相文件**（truth）：AI 写作的权威参考源。包括角色矩阵、章节摘要、伏笔状态、风格画像等。
- **大纲与设定**：Markdown 格式的世界观、规则、大纲文件。

在画布中选中这些资源后，可直接查看和编辑其内容。

---

## 驾驶舱总览

点击资源树中的作品根节点，画布区域显示驾驶舱总览，包含：

| 模块 | 内容 |
|------|------|
| 进度条 | 章节进度百分比（已写/目标章节数） |
| 总字数 | 全书字数统计，含每章均字数 |
| 下一章 | 提示下一章章节号 |
| 审校状态 | 已通过审校章节数、待审章节数 |
| 风险提示 | 审校未通过章节数，红色高亮 |
| 下一步建议 | 根据当前状态给出优先级建议 |

### 驾驶舱底部面板

| 面板 | 功能 |
|------|------|
| 预设配置 | Toggle 启用/禁用各写作预设 |
| 节拍进度 | 选择节拍模板，追踪写作进度 |
| 质量监控 | AI 味趋势图、文风漂移检测、审校通过率 |
| 警告汇总 | 汇总当前作品的所有警告信息 |

---

## 经纬图谱

经纬图谱提供 5 种可视化视图：

| 视图 | 说明 |
|------|------|
| 关系图谱 | 角色/势力之间的关系网络 |
| 角色弧线 | 角色在全书中的成长轨迹与情感变化曲线 |
| 矛盾地图 | 当前活跃的矛盾冲突及其关联角色/事件 |
| 列表视图 | 所有经纬条目的表格化展示 |
| 时间线 | 按时间轴展示事件发生顺序与因果关系 |

---

## 导出

支持将作品导出为以下格式：

| 格式 | 说明 |
|------|------|
| TXT | 纯文本格式 |
| Word | .docx 格式 |
| ePub | 电子书格式 |
