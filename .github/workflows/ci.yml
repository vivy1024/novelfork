# 公开仓库不跑自动 CI。
# 完整验证在维护者本机完成：测试 + 编译 Windows EXE + EXE 功能核验。
# 需要时仅可手动触发本 workflow。
name: CI

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Public CI disabled
        run: |
          echo "Public CI is intentionally disabled."
          echo "Release gate: local tests + Windows EXE + EXE verification."
